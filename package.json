{
  "name": "twinprice-extension",
  "version": "2.1.7",
  "private": true,
  "description": "Privacy-focused Manifest V3 currency conversion extension",
  "scripts": {
    "test": "node --test",
    "prepare:chrome": "node scripts/build.mjs chrome --prepare-only",
    "prepare:firefox": "node scripts/build.mjs firefox --prepare-only",
    "build": "node scripts/build.mjs all",
    "build:chrome": "node scripts/build.mjs chrome",
    "build:firefox": "node scripts/build.mjs firefox",
    "lint:firefox": "npm run prepare:firefox && web-ext lint --source-dir dist/firefox",
    "run:firefox": "npm run prepare:firefox && web-ext run --source-dir dist/firefox",
    "test:browser": "npm run prepare:chrome && playwright test",
    "test:browser:headed": "npm run prepare:chrome && playwright test --headed",
    "test:browser:firefox": "npm run prepare:firefox && node --test tests/firefox/runtime.js",
    "test:live-sites": "npm run prepare:chrome && node scripts/run-live-sites.js",
    "check": "node scripts/check-project.js",
    "verify": "npm run check && npm test",
    "build:icons": "node scripts/build-icons.mjs",
    "build:promo": "node scripts/build-promo-tile.mjs",
    "capture": "npm run prepare:chrome && playwright test --config playwright.capture.config.js",
    "build:store-assets": "npm run capture && node scripts/build-store-screenshots.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "selenium-webdriver": "4.46.0",
    "web-ext": "9.4.0"
  }
}
